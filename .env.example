# ============================================================
# Profile Intake Server – Environment Configuration Example
# ============================================================
# This file documents all environment variables required to run
# the server locally or in a containerized environment.
#
# Copy this file to `.env` and adjust values as needed.
# NEVER commit real secrets to version control.
# ============================================================

# ----------------------------------------------------------------
# Authentication
# ----------------------------------------------------------------
# Static Bearer token used to protect API endpoints.
# This value MUST be overridden in production.
API_TOKEN=dev-token-change-me

# ----------------------------------------------------------------
# Database
# ----------------------------------------------------------------
# SQLAlchemy database URL.
# Default uses a local SQLite database for development.
DATABASE_URL=sqlite:///./data.db

# ----------------------------------------------------------------
# File uploads
# ----------------------------------------------------------------
# Directory where uploaded files will be stored.
# Must be writable by the application process/container.
UPLOAD_DIR=./uploads

# Maximum allowed upload size (in megabytes)
MAX_FILE_SIZE_MB=10

# Comma-separated list of allowed file extensions
ALLOWED_FILE_TYPES=["pdf"]

# ----------------------------------------------------------------
# CORS (Cross-Origin Resource Sharing)
# ----------------------------------------------------------------
# Enable or disable CORS support
ENABLE_CORS=true

# Allowed origins for CORS.
# Use "*" only for local development.
CORS_ORIGINS=["*"]

# ----------------------------------------------------------------
# Logging
# ----------------------------------------------------------------
# Log level for the application.
# Valid values: debug, info, warning, error, critical
LOG_LEVEL=info
